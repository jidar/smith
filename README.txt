Architecture
------------

A lightweight, protocol-independent server that responds on a configured
port with canned responses.

Responds to TCP/UDP crafted packets, as well as a GET call to the remote server.
